// data
const = [10, 2, 4, 5, 20, 35];

//size
const width = 400,
    height = 400,
    radius = width / 2;

// SVG
const svg = d3.select('#pieChart')
    .append('svg')
    .attr('width', width)
    .attr('height', height);

// 準備產生圓形資料的函數


// 準備產生弧形資料的函數